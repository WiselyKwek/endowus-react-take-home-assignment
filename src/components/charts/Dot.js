
const Dot = ({ dotColor }) => {
    return (
        <div style={{ borderRadius: "100%", backgroundColor: dotColor, width:10, height:10, display: 'inline-block', marginRight: 5}}>

        </div>
    )
}

export default Dot